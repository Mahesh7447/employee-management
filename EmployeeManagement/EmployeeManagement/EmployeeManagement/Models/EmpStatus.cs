namespace EmployeeManagement.Models
{
    public enum EmpStatus
    {
        Active,
        Inactive,
        Disabled
    }

    public enum Gender
    {
        Male,
        Female,
        TransGender
    }
}
